name: example-login
status: draft
description: Example Probe (safe to delete). Open login and check form visibility.
base_url: http://localhost:3000
scope: targeted
tags: [example, auth]

steps:
  - id: open
    action: open
    url: /login
  - id: snapshot
    action: snapshot

expect:
  - type: no_console_errors
  - type: visible
    target: "Login"
