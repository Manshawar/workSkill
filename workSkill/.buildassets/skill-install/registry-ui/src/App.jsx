// 骨架文件 —— 业务内容(列表 / 表单 / 分类下拉 / fetch API)我们自己写。
// 此阶段仅验证工程链通(npm run build 能产出 dist/),不写任何 registry 业务。
// 业务开发前置条件:DESIGN.md §九 阶段 1、阶段 2(命令层)全绿后,再在此填 UI 业务。
export default function App() {
  return (
    <div>
      <h1>Skill Installer · Registry</h1>
      <p>骨架占位 —— UI 业务待写(见 ../DESIGN.md §九)。</p>
    </div>
  )
}
